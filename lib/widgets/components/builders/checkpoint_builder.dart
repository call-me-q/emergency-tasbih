part of '../components.dart';

class CheckpointBuilder extends StatelessWidget {
  const CheckpointBuilder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
